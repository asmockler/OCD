'use strict';
var React = require('react-native');
var {
  StyleSheet,
} = React;

var text = StyleSheet.create({
  movingText: {
    fontSize: 50,
    textAlign: 'center'
  }
})

module.exports = text;
